@model hotelreservation.Models.tbl_room

@{
    ViewBag.Title = "deleteroom";
    Layout = "~/Views/Shared/_AdminLayoutPage.cshtml";
}

<h2>deleteroom</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>tbl_room</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.no_of_room)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.no_of_room)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.tbl_roomtype.room_type)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.tbl_roomtype.room_type)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "viewroom")
        </div>
    }
</div>
